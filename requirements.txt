# ====================================
# AutoTrade Pro v3.0 - 필수 패키지
# ====================================

# 핵심 패키지
requests>=2.31.0
urllib3>=2.0.0

# 데이터 처리 (Windows 호환 버전 - pre-built wheels 사용)
pandas>=2.2.0
numpy>=1.26.0,<2.0.0

# 웹 프레임워크 (대시보드)
Flask>=3.0.0
Werkzeug>=3.0.0
flask-socketio>=5.3.0
python-socketio>=5.10.0

# WebSocket
websocket-client>=1.7.0

# AI 분석
google-generativeai>=0.3.0

# 환경변수 관리
python-dotenv>=1.0.0

# 설정 관리
PyYAML>=6.0.0
pydantic>=2.5.0  # v4.2: Type-safe configuration (also used by FastAPI)

# 로깅
colorlog>=6.8.0
loguru>=0.7.0

# 데이터베이스
SQLAlchemy>=2.0.0

# 유틸리티
pytz>=2023.3
python-dateutil>=2.8.0

# ====================================
# v3.0 신규 패키지
# ====================================

# CORS 지원
flask-cors>=4.0.0

# 비동기 HTTP (성능 최적화)
httpx>=0.25.0
aiohttp>=3.9.0

# 캐싱 (Redis)
redis>=5.0.0
hiredis>=2.2.0  # Redis 성능 향상

# 백그라운드 작업 큐
rq>=1.15.0

# 데이터 직렬화
msgpack>=1.0.7

# 기계학습 (ML 예측)
scikit-learn>=1.3.0
xgboost>=2.0.0

# ====================================
# v4.0 Comprehensive Enhancement
# ====================================

# FastAPI for REST API
fastapi>=0.104.0
uvicorn>=0.24.0

# 고급 백테스팅 리포트 (HTML/PDF 생성)
jinja2>=3.1.0
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.18.0
kaleido>=0.2.1  # Plotly 이미지 export
weasyprint>=60.0  # HTML to PDF
reportlab>=4.0.0  # PDF 생성

# 파라미터 최적화
optuna>=3.4.0  # Bayesian Optimization
scikit-optimize>=0.9.0  # 추가 최적화 도구

# 이상 감지
pyod>=1.1.0  # Outlier Detection
statsmodels>=0.14.0  # 통계 분석

# 기술적 지표
# ta-lib은 Windows에서 설치 어려움 (numpy 빌드 의존성 필요)
# ta-lib  # Technical Analysis Library (선택적) - 주석 처리, pandas-ta 사용 권장
pandas-ta>=0.3.14b  # Pandas Technical Analysis (ta-lib 대체 가능)

# 차트 및 시각화
# chart.js는 JavaScript 라이브러리이므로 Python requirements에서 제외
# 웹 대시보드에서 CDN을 통해 사용: <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

# WebSocket 강화
python-engineio>=4.8.0

# 스케줄링
apscheduler>=3.10.0

# 데이터 검증
cerberus>=1.3.5

# 암호화 (API 키 보안)
cryptography>=41.0.0

# 추가 유틸리티
more-itertools>=10.1.0
tqdm>=4.66.0  # 진행률 표시

# ====================================
# 선택적 패키지 (설치 실패 시 주석 처리)
# ====================================

# 기술적 분석 (ta 패키지 대신 pandas-ta 사용 권장)
# ta>=0.11.0

# DB 마이그레이션 (고급 기능)
# alembic>=1.13.0

# Deep Learning (LSTM 예측)
# tensorflow>=2.15.0
# torch>=2.1.0

# ====================================
# v6.0 Performance & Security
# ====================================

# Web scraping (뉴스 크롤링)
beautifulsoup4>=4.12.0
lxml>=5.1.0

# Performance optimization (선택적)
numba>=0.58.0  # JIT compilation - 성능 100배 향상

# System monitoring
psutil>=5.9.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# AI Providers (선택적)
# anthropic>=0.8.0  # Claude AI
# openai>=1.6.0  # GPT-4

# ====================================
# Kiwoom Open API
# ====================================

# koapy - 키움 Open API wrapper (64비트 Python에서 사용)
# protobuf 3.20.x 필요 (4.x와 호환 안 됨)
protobuf==3.20.3
grpcio==1.54.2  # 안정 버전 (1.48.0은 yanked됨 - 데드락 이슈)
koapy>=0.5.0

# pywin32 - Windows COM/ActiveX 지원 (64bit-kiwoom-openapi 직접 사용 시)
# pip install pywin32 별도 실행 필요
