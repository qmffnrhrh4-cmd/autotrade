# ====================================
# AutoTrade - 64-bit Python 3.13 Requirements
# ====================================
# 이 파일은 64비트 환경(main.py 실행용)을 위한 패키지 목록입니다.

# 핵심 패키지
requests>=2.31.0
urllib3>=2.0.0

# 데이터 처리 (64비트는 최신 버전 사용 가능)
pandas>=2.2.0
numpy>=1.26.0,<2.0.0

# 웹 프레임워크 (대시보드)
Flask>=3.0.0
Werkzeug>=3.0.0
flask-socketio>=5.3.0
python-socketio>=5.10.0
flask-cors>=4.0.0

# WebSocket
websocket-client>=1.7.0

# AI 분석
google-generativeai>=0.3.0

# 환경변수 관리
python-dotenv>=1.0.0

# 설정 관리
PyYAML>=6.0.0
pydantic>=2.5.0

# 로깅
colorlog>=6.8.0
loguru>=0.7.0

# 데이터베이스
SQLAlchemy>=2.0.0

# 유틸리티
pytz>=2023.3
python-dateutil>=2.8.0

# 비동기 HTTP (성능 최적화)
httpx>=0.25.0
aiohttp>=3.9.0

# 캐싱 (Redis) - 선택 사항
redis>=5.0.0
hiredis>=2.2.0

# 백그라운드 작업 큐
rq>=1.15.0

# 데이터 직렬화
msgpack>=1.0.7

# 기계학습 (ML 예측) - 64비트 추천
scikit-learn>=1.3.0
xgboost>=2.0.0

# FastAPI for REST API
fastapi>=0.104.0
uvicorn>=0.24.0

# 백테스팅 리포트 (HTML/PDF 생성)
jinja2>=3.1.0
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.18.0

# 파라미터 최적화
optuna>=3.4.0

# 이상 감지
statsmodels>=0.14.0

# 기술적 지표
pandas-ta>=0.3.14b

# WebSocket 강화
python-engineio>=4.8.0

# 스케줄링
apscheduler>=3.10.0

# 데이터 검증
cerberus>=1.3.5

# 암호화 (API 키 보안)
cryptography>=41.0.0

# 추가 유틸리티
more-itertools>=10.1.0
tqdm>=4.66.0

# Web scraping (뉴스 크롤링)
beautifulsoup4>=4.12.0
lxml>=5.1.0

# Performance optimization
numba>=0.58.0

# System monitoring
psutil>=5.9.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# ====================================
# NOTE: OpenAPI 관련 패키지는 제외
# ====================================
# PyQt5, koapy, protobuf 등은 32비트 환경(autotrade_32)에만 설치됩니다.
# 64비트 환경에서는 HTTP 클라이언트로 32비트 서버와 통신합니다.
